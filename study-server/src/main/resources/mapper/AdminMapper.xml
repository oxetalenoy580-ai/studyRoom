<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.mapper.AdminMapper">

  <select id="queryUserList" resultType="com.study.entity.User">
    select id, username, password, name, phone, create_time
    from user
    <where>
      <if test="queryDTO.username != null and queryDTO.username != ''">
        and username like concat('%', #{queryVO.username}, '%')
      </if>
      <if test="queryDTO.name != null and queryDTO.name != ''">
        and name like concat('%', #{queryVO.name}, '%')
      </if>
      <if test="queryDTO.phone != null and queryDTO.phone != ''">
        and phone like concat('%', #{queryVO.phone}, '%')
      </if>
    </where>
    order by create_time desc
    limit #{pageNum}, #{pageSize}
  </select>

  <select id="queryUserCount" resultType="java.lang.Long">
    select count(*)
    from user
    <where>
      <if test="queryDTO.username != null and queryDTO.username != ''">
        and username like concat('%', #{queryVO.username}, '%')
      </if>
      <if test="queryDTO.name != null and queryDTO.name != ''">
        and name like concat('%', #{queryVO.name}, '%')
      </if>
      <if test="queryDTO.phone != null and queryDTO.phone != ''">
        and phone like concat('%', #{queryVO.phone}, '%')
      </if>
    </where>
  </select>

</mapper>
